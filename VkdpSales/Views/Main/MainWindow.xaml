<Window x:Class="VkdpSales.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:VkdpSales.ViewModels"
        xmlns:views="clr-namespace:VkdpSales.Views.UserControls"
        xmlns:local="clr-namespace:VkdpSales"
        mc:Ignorable="d"
        Title="MainWindow" Height="750" Width="1200">
    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:NewSaleViewModel}">
            <views:NewSaleView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:SalesHistoryViewModel}">
            <views:SalesHistoryView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ClientsViewModel}">
            <views:ClientsView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ProductsViewModel}">
            <views:ProductsView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:AnalyticsViewModel}">
            <views:AnalyticsView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:UsersViewModel}">
            <views:UsersView/>
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="4"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- 🔹 Левая панель: оформленная, с тенью и градиентом -->
        <Border Grid.Column="0" 
                Background="{StaticResource GreenGradient}"
                BorderBrush="#27AE60"
                BorderThickness="0,0,1,0"
                CornerRadius="0,8,8,0">
            <Border.Effect>
                <DropShadowEffect Color="#2ECC71" BlurRadius="20" ShadowDepth="2" Opacity="0.15"/>
            </Border.Effect>

            <StackPanel Margin="0,40,0,20">
                <!-- Логотип -->
                <Border Background="#27AE60" Padding="0,15" CornerRadius="0,6,6,0" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="🏭 ВКДП" Foreground="White" FontSize="20" FontWeight="Bold"
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="Учёт продаж" Foreground="#E8F5E9" FontSize="11"
                                   HorizontalAlignment="Center" Margin="0,2,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Информация о пользователе -->
               
                <!-- 🔘 Кнопки меню -->
                <!-- 🔘 Кнопки меню с управлением видимости по ролям -->
                <StackPanel Margin="8,0,8,0">

                    <!-- 🛒 Новая продажа: Менеджер + Админ -->
                    <Button Content="🛒 Новая продажа" Command="{Binding NewSaleCommand}">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource SidebarButton}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsManager}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <!-- 📋 История продаж: Все роли -->
                    <Button Content="📋 История продаж" Command="{Binding SalesHistoryCommand}" 
            Style="{StaticResource SidebarButton}"/>

                    <!-- 📦 Товары: Менеджер + Админ -->
                    <Button Content="📦 Товары" Command="{Binding ProductsCommand}">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource SidebarButton}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsManager}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <!-- 👥 Клиенты: Менеджер + Админ -->
                    <Button Content="👥 Клиенты" Command="{Binding ClientsCommand}">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource SidebarButton}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsManager}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <!-- 📊 Статистика: Аналитик + Админ -->
                    <Button Content="📊 Статистика" Command="{Binding AnalyticsCommand}">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource SidebarButton}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsAnalyst}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <!-- 👤 Пользователи: Только Админ -->
                    <Button Content="👤 Пользователи" Command="{Binding UsersCommand}">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource SidebarButton}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsAdmin}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                </StackPanel>

                <!-- Разделитель -->
                <Rectangle Height="1" Fill="#3DD484" Margin="15,10" Opacity="0.5"/>

                <!-- Кнопка выхода: всем -->
                <Button Content="🚪 Выйти" Command="{Binding LogoutCommand}" 
        Style="{StaticResource SidebarButton}" Margin="8,10,8,0"/>
            </StackPanel>
        </Border>

        <!-- 🔹 Вертикальный разделитель (визуальная линия) -->
        <Border Grid.Column="1" Background="#27AE60" Opacity="0.3"/>

        <!-- 🔹 Область контента -->
        <Border Grid.Column="2" Background="{StaticResource WindowBackground}">
            <ContentControl Content="{Binding CurrentView}" Margin="25"/>
        </Border>
    </Grid>
</Window>
