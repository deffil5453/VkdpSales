<UserControl x:Class="VkdpSales.Views.UserControls.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VkdpSales.Views.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 1. Верхние карточки с метриками -->
        <StackPanel Grid.Row="0" Orientation="Horizontal">

            <!-- Карточка Выручки -->
            <Border Background="White" CornerRadius="8" Padding="20" Width="250" Margin="0,0,20,0">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.1"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="💰 Выручка сегодня" Foreground="#7F8C8D" FontSize="14" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding TodayRevenue, StringFormat={}{0:N0} ₽}" 
                               FontSize="28" FontWeight="Bold" Foreground="#27AE60" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>

            <!-- Карточка Заказов -->
            <Border Background="White" CornerRadius="8" Padding="20" Width="250">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.1"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="📦 Заказов сегодня" Foreground="#7F8C8D" FontSize="14" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding TodayOrders}" 
                               FontSize="28" FontWeight="Bold" Foreground="#3498DB" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>
        </StackPanel>

        <!-- 2. Таблица Топ-5 товаров -->
        <Border Grid.Row="2" Background="White" CornerRadius="8" Padding="20">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.1"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="🏆 Топ-5 ходовых товаров" FontSize="20" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,15"/>

                <DataGrid Grid.Row="1" ItemsSource="{Binding TopProducts}" AutoGenerateColumns="False" 
                          IsReadOnly="True" Background="Transparent" BorderThickness="0"
                          AlternatingRowBackground="#F8FBF9">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Товар" Binding="{Binding Name}" Width="2*"/>
                        <DataGridTextColumn Header="Категория" Binding="{Binding Category}" Width="1*"/>
                        <DataGridTextColumn Header="Продано (шт)" Binding="{Binding QuantitySold}" Width="1*">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Выручка" Binding="{Binding TotalRevenue, StringFormat={}{0:N0} ₽}" Width="1*">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Right"/>
                                    <Setter Property="Foreground" Value="#27AE60"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
