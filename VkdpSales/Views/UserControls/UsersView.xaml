<UserControl x:Class="VkdpSales.Views.UserControls.UsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VkdpSales.Views.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <!-- Таблица пользователей -->
        <DataGrid Grid.Column="0" ItemsSource="{Binding Users}" SelectedItem="{Binding SelectedUser}"
                  AutoGenerateColumns="False" IsReadOnly="True" AlternatingRowBackground="#F5F5F5" 
                  GridLinesVisibility="Horizontal" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Логин" Binding="{Binding Login}" Width="1*"/>
                <DataGridTextColumn Header="ФИО" Binding="{Binding FullName}" Width="2*"/>
                <DataGridTextColumn Header="Роль" Binding="{Binding Role.Name}" Width="1*"/>
                <DataGridTextColumn Header="Активен" Binding="{Binding IsActive}" Width="0.8*"/>
                <DataGridTextColumn Header="Создан" Binding="{Binding CreatedAt, StringFormat=dd.MM.yyyy}" Width="1.2*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Форма -->
        <Border Grid.Column="2" Background="#F8F9FA" Padding="15" CornerRadius="5">
            <StackPanel>
                <TextBlock Text="{Binding FormTitle}" FontSize="16" FontWeight="Bold" Margin="0,0,0,15" Foreground="#1B4D3E"/>

                <TextBlock Text="Логин*:" Style="{StaticResource LabelStyle}"/>
                <TextBox Text="{Binding Login}" Margin="0,0,0,10"/>

                <TextBlock Text="Пароль:" Style="{StaticResource LabelStyle}"/>
                <PasswordBox x:Name="PwdBox" Margin="0,0,0,10"/>

                <TextBlock Text="ФИО*:" Style="{StaticResource LabelStyle}"/>
                <TextBox Text="{Binding FullName}" Margin="0,0,0,10"/>

                <TextBlock Text="Роль:" Style="{StaticResource LabelStyle}"/>
                <ComboBox ItemsSource="{Binding Roles}" SelectedItem="{Binding SelectedRole}" Margin="0,0,0,10"/>

                <CheckBox Content="Активен" IsChecked="{Binding IsActive}" Margin="0,0,0,15"/>

                <!-- Кнопки Добавить/Изменить -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <Button Content="Добавить" Command="{Binding AddCommand}" Width="80" Height="30" Margin="0,0,5,0" Style="{StaticResource GreenButton}"/>
                    <Button Content="Изменить" Command="{Binding EditCommand}" Width="80" Height="30" Style="{StaticResource BlueButton}"/>
                </StackPanel>

                <!-- Кнопки Сохранить/Отмена (только при редактировании) -->
                <StackPanel Orientation="Horizontal">
                    <StackPanel.Style>
                        <Style TargetType="StackPanel">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsEditMode}" Value="True">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </StackPanel.Style>
                    <Button Content="💾 Сохранить" 
                        Command="{Binding SaveCommand}" 
                        CommandParameter="{Binding ElementName=PwdBox}"
                        Width="90" Height="30" Margin="0,0,5,0" Style="{StaticResource GreenButton}"/>  
                    <Button Content="❌ Отмена" Command="{Binding CancelCommand}" Width="90" Height="30" Style="{StaticResource OutlineButton}"/>
                </StackPanel>

                <Button Content="Удалить" Command="{Binding DeleteCommand}" Width="100" Height="30" Margin="0,10,0,0" Style="{StaticResource DangerButton}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
