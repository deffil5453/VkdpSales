<UserControl x:Class="VkdpSales.Views.UserControls.ClientsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VkdpSales.Views.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="900">
    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <!-- Таблица клиентов -->
        <DataGrid Grid.Column="0" ItemsSource="{Binding Clients}" SelectedItem="{Binding SelectedClient}"
                  AutoGenerateColumns="False" IsReadOnly="True" AlternatingRowBackground="#F5F5F5" 
                  GridLinesVisibility="Horizontal" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Тип" Binding="{Binding Type}" Width="0.6*"/>
                <DataGridTextColumn Header="Название / ФИО" Binding="{Binding Name}" Width="2*"/>
                <DataGridTextColumn Header="ИНН" Binding="{Binding INN}" Width="1*"/>
                <DataGridTextColumn Header="Телефон" Binding="{Binding Phone}" Width="1*"/>
                <DataGridTextColumn Header="Скидка" Binding="{Binding DiscountPercent, StringFormat={}{0:F0}%}" Width="0.6*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Форма редактирования/добавления -->
        <Border Grid.Column="2" Background="#F8F9FA" Padding="15" CornerRadius="5">
            <StackPanel>
                <TextBlock Text="{Binding FormTitle}" FontSize="16" FontWeight="Bold" Margin="0,0,0,15" Foreground="#2C3E50"/>

                <TextBlock Text="Тип клиента:" Margin="0,0,0,5"/>
                <ComboBox ItemsSource="{Binding ClientTypes}" SelectedItem="{Binding Type}" Height="35" Margin="0,0,0,10"/>

                <TextBlock Text="Название / ФИО*:" Margin="0,0,0,5"/>
                <TextBox Text="{Binding Name}" Height="35" Margin="0,0,0,10"/>

                <TextBlock Text="ИНН (для юрлиц):" Margin="0,0,0,5"/>
                <TextBox Text="{Binding INN}" Height="35" Margin="0,0,0,10"/>

                <TextBlock Text="Телефон*:" Margin="0,0,0,5"/>
                <TextBox Text="{Binding Phone}" Height="35" Margin="0,0,0,10"/>

                <TextBlock Text="Email:" Margin="0,0,0,5"/>
                <TextBox Text="{Binding Email}" Height="35" Margin="0,0,0,10"/>

                <TextBlock Text="Адрес:" Margin="0,0,0,5"/>
                <TextBox Text="{Binding Address}" Height="35" Margin="0,0,0,10"/>

                <TextBlock Text="Скидка (%):" Margin="0,0,0,5"/>
                <TextBox Text="{Binding Discount}" Height="35" Margin="0,0,0,15"/>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <Button Content="Добавить" Command="{Binding AddCommand}" Width="80" Height="30" Margin="0,0,5,0" Background="#27AE60" Foreground="White"/>
                    <Button Content="Изменить" Command="{Binding EditCommand}" Width="80" Height="30" Background="#2980B9" Foreground="White"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal">
                    <StackPanel.Style>
                        <Style TargetType="StackPanel">
                            <!-- По умолчанию скрыто -->
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <!-- Показываем, если IsEditMode = true -->
                                <DataTrigger Binding="{Binding IsEditMode}" Value="True">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </StackPanel.Style>

                    <!-- ✅ Кнопки "Сохранить" и "Отмена" -->
                    <Button Content="💾 Сохранить" Command="{Binding SaveCommand}" 
            Width="90" Height="30" Margin="0,0,5,0" 
            Background="#16A085" Foreground="White" Cursor="Hand"/>
                    <Button Content="❌ Отмена" Command="{Binding CancelCommand}" 
            Width="90" Height="30" 
            Background="#7F8C8D" Foreground="White" Cursor="Hand"/>
                </StackPanel>

                <Button Content="Удалить" Command="{Binding DeleteCommand}" Width="100" Height="30" Background="#C0392B" Foreground="White" Margin="0,10,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
